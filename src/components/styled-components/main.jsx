import Styled from 'styled-components'

export const StyledMain = Styled.main`
    width: calc(100% - 40px);
    max-width: 1200px;
    height: auto;
    min-height: calc(100vh - 142px);
    margin: auto;
    padding: 20px;
`